{
  "name": "stacky-health",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "build": "amdxg build:index && amdxg build:history && node script/build-tags.mjs && next build && next export",
    "dev": "amdxg build:index && amdxg build:history && node script/build-tags.mjs && next",
    "generate-ogp": "node script/generate-ogp.mjs",
    "new-page": "amdxg new:page"
  },
  "dependencies": {
    "amdx": "^0.9.0",
    "amdx-runner": "^0.9.0",
    "date-fns": "^2.23.0",
    "next": "latest",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.19",
    "amdx-loader": "^0.9.0",
    "amdxg-cli": "^0.9.4",
    "typescript": "^4.4.2"
  },
  "optionalDependencies": {
    "canvas": "^2.8.0"
  }
}
